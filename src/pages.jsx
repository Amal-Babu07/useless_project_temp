import { useMemo, useState } from 'react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { Area, AreaChart, Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { ArrowRight, CalendarDays, Check, Download, Heart, Medal, Printer, RotateCcw, Shield, Sparkles, Target, TrendingUp, Users } from 'lucide-react';
import { AssessmentQuestion, CategoryScore, ChartCard, FeatureIcon, FriendshipCard, ProgressBar, ScoreCircle, StepButtons } from './components';
import { questions, leaderboard } from './data';
import { calculateScore } from './scoring';
import { generateAnalysis } from './analysis';
import { createId, formatDate, saveAssessment } from './utils';

const categoryLabels = { communication: 'Communication', trust: 'Trust', support: 'Support', reliability: 'Reliability', fun: 'Fun', effort: 'Effort' };
const categoryIcons = { communication: '◌', trust: '◈', support: '♡', reliability: '↗', fun: '✦', effort: '⚡' };

export function Home() { return <div><section className="hero container"><div className="hero-copy reveal"><span className="eyebrow accent">THE FRIENDSHIP CREDIT SCORE</span><h1>Good friends are<br /><em>worth measuring.</em></h1><p className="hero-lede">SYNRGY turns the invisible parts of your closest friendships into a score you can understand, celebrate, and grow.</p><div className="hero-actions"><Link className="button primary" to="/assessment">Check friendship score <ArrowRight size={17} /></Link><Link className="text-link" to="/dashboard">Explore the dashboard <ArrowRight size={15} /></Link></div><div className="hero-proof"><div className="proof-avatars"><span>MC</span><span>JL</span><span>AR</span><span>+2k</span></div><span>2,400+ bonds measured<br /><b>and counting</b></span></div></div><div className="hero-visual reveal-delay"><div className="orbit orbit-one" /><div className="orbit orbit-two" /><div className="hero-score"><span className="eyebrow">LATEST BOND</span><div className="hero-score-main"><strong>92</strong><span>/ 100</span></div><div className="hero-score-line"><span><i className="status-dot" /> Soulmate Friend</span><span>+8.4%</span></div><ProgressBar value={92} /></div><div className="floating-note note-top"><span className="note-icon">✦</span><span>Trust is strong<br /><b>94 / 100</b></span></div><div className="floating-note note-bottom"><span className="note-icon warm">♡</span><span>Made for<br /><b>real friends</b></span></div></div></section><section className="home-intro"><div className="container intro-grid"><div><span className="eyebrow">THE SYNRGY METHOD</span><h2>A clearer look at<br /><em>what connects you.</em></h2></div><p>Every score is built from the things that make a friendship feel like home: showing up, being honest, making time, and having a great time while you do it.</p></div><div className="container feature-grid"><div className="feature-item"><FeatureIcon type="trust" /><h3>Six signals</h3><p>Trust, support, communication, reliability, fun, and effort shape every score.</p></div><div className="feature-item"><FeatureIcon type="support" /><h3>Built to reflect</h3><p>A gentle snapshot that opens better conversations, never a final verdict.</p></div><div className="feature-item"><FeatureIcon type="energy" /><h3>Private by default</h3><p>Your assessments live in your browser. No account, no awkward data trail.</p></div></div></section></div>; }

export function Assessment({ setAssessments }) { const navigate = useNavigate(); const [step, setStep] = useState(-1); const [friendName, setFriendName] = useState(''); const [answers, setAnswers] = useState(Object.fromEntries(questions.map((q) => [q.id, 70]))); const current = questions[step];
  function finish() { const result = calculateScore(answers); const assessment = { ...result, answers, friendName: friendName.trim() || 'Your friend', id: createId(), date: new Date().toISOString(), strengths: generateAnalysis({ ...result, answers }).strengths, improvement: generateAnalysis({ ...result, answers }).improvement }; saveAssessment(assessment); setAssessments((items) => [assessment, ...items]); navigate(`/result/${assessment.id}`); }
  if (step === -1) return <div className="assessment-intro container narrow reveal"><div className="eyebrow accent">A TWO-MINUTE CHECK-IN</div><h1>Let's measure a<br /><em>meaningful bond.</em></h1><p>Answer honestly, there are no wrong answers. This is a conversation starter, wrapped in a little data.</p><label className="field-label" htmlFor="friend-name">Who are you measuring?</label><input id="friend-name" className="text-input" value={friendName} onChange={(event) => setFriendName(event.target.value)} placeholder="Their name" autoFocus /><button className="button primary full" onClick={() => setStep(0)}>Start assessment <ArrowRight size={17} /></button><p className="fine-print"><Shield size={13} /> Your answers stay on this device.</p></div>;
  return <div className="assessment-page container reveal"><div className="assessment-top"><div><span className="eyebrow">ASSESSING {friendName || 'YOUR FRIEND'}</span><h1>Read between<br /><em>the lines.</em></h1></div><span className="step-count">{String(step + 1).padStart(2, '0')} <small>/ 10</small></span></div><div className="assessment-progress"><ProgressBar value={((step + 1) / questions.length) * 100} /><span>{Math.round(((step + 1) / questions.length) * 100)}% complete</span></div><AssessmentQuestion question={current} value={answers[current.id]} onChange={(value) => setAnswers({ ...answers, [current.id]: value })} /><StepButtons onBack={step > 0 ? () => setStep(step - 1) : undefined} onNext={() => step === questions.length - 1 ? finish() : setStep(step + 1)} isLast={step === questions.length - 1} /></div>;
}

export function Result({ assessments }) { const { id } = useParams(); const item = assessments.find((entry) => entry.id === id) || assessments[0]; const analysis = generateAnalysis(item); return <div className="result-page container reveal"><div className="result-heading"><div><span className="eyebrow accent">YOUR SYNRGY RESULT</span><h1>{item.friendName} is a<br /><em>{item.level.toLowerCase()}.</em></h1><p>Measured {formatDate(item.date)} <span className="dot">·</span> {item.id}</p></div><div className="result-actions"><Link className="button secondary" to={`/card/${item.id}`}><Download size={16} /> Friendship card</Link><Link className="button ghost" to="/assessment"><RotateCcw size={16} /> New score</Link></div></div><div className="result-overview"><div className="score-block"><ScoreCircle score={item.score} /><div><span className="eyebrow">FRIENDSHIP LEVEL</span><h2>{item.level}</h2><p>Your bond is showing real momentum. Keep tending to what makes it yours.</p></div></div><div className="analysis-block"><div className="analysis-label"><Sparkles size={15} /> SYNRGY ANALYSIS</div><p>{analysis.analysis}</p></div></div><div className="result-grid"><section className="result-categories"><div className="section-heading"><div><span className="eyebrow">THE BREAKDOWN</span><h2>What makes it work</h2></div><span className="muted-note">Weighted score</span></div>{Object.entries(item.categories).map(([key, value]) => <CategoryScore key={key} label={categoryLabels[key]} value={value} icon={categoryIcons[key]} />)}</section><aside className="insight-column"><div className="insight-card strength"><span className="eyebrow">MAIN STRENGTHS</span><h3>The bright spots</h3>{analysis.strengths.map((strength) => <div className="insight-line" key={strength}><Check size={15} /> {strength}</div>)}</div><div className="insight-card improve"><span className="eyebrow">ONE AREA TO GROW</span><h3>{analysis.improvement}</h3><p>Small, consistent actions can make this part of your friendship feel even more natural.</p></div><div className="tip-card"><span className="tip-star">✦</span><div><span className="eyebrow">A TIP FOR YOU TWO</span><p>{analysis.tip}</p></div></div></aside></div></div>; }

export function FriendshipCardPage({ assessments }) { const { id } = useParams(); const item = assessments.find((entry) => entry.id === id) || assessments[0]; const analysis = generateAnalysis(item); return <div className="card-page container reveal"><div className="page-heading centered"><span className="eyebrow accent">A LITTLE SOMETHING TO KEEP</span><h1>Make the score<br /><em>official.</em></h1><p>A shareable snapshot of a bond worth celebrating.</p></div><div className="friendship-credit-card"><div className="fcc-top"><span className="brand small"><span className="brand-mark">S</span> SYNRGY</span><span className="fcc-chip">✦</span></div><div className="fcc-score"><span className="eyebrow">FRIENDSHIP CREDIT SCORE</span><strong>{item.score}</strong><span>/ 100</span></div><div className="fcc-bottom"><div><span className="fcc-label">FOR</span><strong>{item.friendName}</strong></div><div><span className="fcc-label">LEVEL</span><strong>{item.level}</strong></div><div><span className="fcc-label">ID / DATE</span><strong>{item.id} · {formatDate(item.date)}</strong></div></div></div><div className="card-details"><div><span className="eyebrow">WHAT MAKES IT YOURS</span><h2>{analysis.strengths.join(' · ')}</h2></div><button className="button primary" onClick={() => window.print()}><Printer size={16} /> Print / download card</button></div></div>; }

export function Dashboard({ assessments }) {
  const average = Math.round(assessments.reduce((sum, item) => sum + item.score, 0) / assessments.length);
  const highest = Math.max(...assessments.map((item) => item.score));
  const history = [...assessments].reverse().map((item, index) => ({ name: `W${index + 1}`, score: item.score }));
  const categories = Object.keys(categoryLabels).map((key) => ({ name: categoryLabels[key].slice(0, 5), score: Math.round(assessments.reduce((sum, item) => sum + (item.categories[key] || 0), 0) / assessments.length) }));
  const levelCounts = assessments.reduce((acc, item) => { acc[item.level] = (acc[item.level] || 0) + 1; return acc; }, {});
  const levelData = Object.entries(levelCounts).map(([name, value]) => ({ name: name.replace(' Friend', ''), value }));
  return <div className="dashboard-page container reveal"><div className="page-heading"><div><span className="eyebrow accent">YOUR FRIENDSHIP LEDGER</span><h1>See the shape of<br /><em>your connections.</em></h1></div><Link className="button primary" to="/assessment">New assessment <ArrowRight size={16} /></Link></div><div className="stat-grid"><div className="stat-card"><Users size={18} /><span>Total assessments</span><strong>{assessments.length}</strong><small>friendships tracked</small></div><div className="stat-card"><TrendingUp size={18} /><span>Average score</span><strong>{average}</strong><small>out of 100</small></div><div className="stat-card"><Medal size={18} /><span>Highest score</span><strong>{highest}</strong><small>your strongest bond</small></div><div className="stat-card highlight"><Heart size={18} /><span>Recent friendship</span><strong>{assessments[0].friendName}</strong><small>{assessments[0].level}</small></div></div><div className="charts-grid"><ChartCard title="Score history" eyebrow="OVER TIME" className="wide-chart"><ResponsiveContainer width="100%" height={240}><AreaChart data={history}><defs><linearGradient id="scoreFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#8c7cff" stopOpacity={0.35} /><stop offset="100%" stopColor="#8c7cff" stopOpacity={0} /></linearGradient></defs><CartesianGrid stroke="#282a48" vertical={false} /><XAxis dataKey="name" stroke="#777993" tickLine={false} axisLine={false} /><YAxis domain={[0, 100]} stroke="#777993" tickLine={false} axisLine={false} /><Tooltip contentStyle={{ background: '#191b35', border: '1px solid #35385d', borderRadius: 8, color: '#fff' }} /><Area type="monotone" dataKey="score" stroke="#9a8cff" strokeWidth={3} fill="url(#scoreFill)" /></AreaChart></ResponsiveContainer></ChartCard><ChartCard title="Category comparison" eyebrow="AVERAGE SCORES"><ResponsiveContainer width="100%" height={240}><BarChart data={categories} barSize={20}><CartesianGrid stroke="#282a48" vertical={false} /><XAxis dataKey="name" stroke="#777993" tickLine={false} axisLine={false} /><YAxis domain={[0, 100]} hide /><Tooltip contentStyle={{ background: '#191b35', border: '1px solid #35385d', borderRadius: 8 }} /><Bar dataKey="score" radius={[5, 5, 0, 0]} fill="#ff9672" /></BarChart></ResponsiveContainer></ChartCard><ChartCard title="Friendship levels" eyebrow="YOUR MIX"><div className="level-chart"><ResponsiveContainer width="46%" height={170}><PieChart><Pie data={levelData} dataKey="value" nameKey="name" innerRadius={48} outerRadius={72} paddingAngle={4}><Cell fill="#9a8cff" /><Cell fill="#6a9cff" /><Cell fill="#ff9672" /><Cell fill="#7c7f9d" /></Pie></PieChart></ResponsiveContainer><div className="level-legend">{levelData.map((entry, index) => <div key={entry.name}><i style={{ background: ['#9a8cff', '#6a9cff', '#ff9672', '#7c7f9d'][index % 4] }} />{entry.name}<b>{entry.value}</b></div>)}</div></div></ChartCard></div><section className="records-section"><div className="section-heading"><div><span className="eyebrow">ALL RECORDS</span><h2>Recent friendships</h2></div><span className="muted-note">Stored locally in your browser</span></div><div className="records-grid">{assessments.map((item) => <FriendshipCard key={item.id} item={item} />)}</div></section></div>;
}

export function Leaderboard() { return <div className="leaderboard-page container reveal"><div className="page-heading centered"><span className="eyebrow accent">THE SYNRGY INDEX</span><h1>Good bonds<br /><em>rise together.</em></h1><p>A little friendly competition for the friendships that go the distance.</p></div><section className="leaderboard"><div className="leaderboard-head"><span>RANK</span><span>FRIENDSHIP</span><span>SCORE</span><span>LEVEL</span></div>{leaderboard.map((item, index) => <div className={`leader-row ${index < 3 ? 'top-rank' : ''}`} key={item.names}><div className={`rank rank-${index + 1}`}>{index < 3 ? <Medal size={17} /> : `0${index + 1}`}</div><div className="leader-name"><div className={`pair-avatar ${item.color}`}>{item.names.split(' & ').map((name) => name[0]).join('')}</div><strong>{item.names}</strong></div><strong className="leader-score">{item.score}</strong><span className="leader-level">{item.level}</span></div>)}</section><div className="leader-footer"><Target size={18} /><span>Scores are a snapshot, not a verdict. The best friendship is the one you keep showing up for.</span><Link className="text-link" to="/assessment">Measure yours <ArrowRight size={15} /></Link></div></div>; }
